<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:BabyMobile.ViewModels"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="BabyMobile.Views.RegisterPage">
    
    <ContentPage.BindingContext>
        <vm:RegisterViewModel/>
    </ContentPage.BindingContext>

    <StackLayout Orientation="Vertical">
        
        <Label Text="Welcome to BabyLife!" 
           HorizontalOptions="Center"
           VerticalOptions="CenterAndExpand" />
        
        <Entry Text="{Binding Email}" Placeholder="email"></Entry>
        <Entry Text="{Binding Year}" Placeholder="birthday"></Entry>
        <Entry Text="{Binding Password}" Placeholder="password" IsPassword="True"></Entry>
        <Entry Text="{Binding PasswordConfirm}" Placeholder="confirm password" IsPassword="True"></Entry>

        <Button Command="{Binding RegisterCommand}"
                Text="Register"/>

        <Label Text="{Binding Message}"/>

        <Button Text="Go to Login Page"
                Clicked="Button_Clicked"/>

    </StackLayout>
</ContentPage>