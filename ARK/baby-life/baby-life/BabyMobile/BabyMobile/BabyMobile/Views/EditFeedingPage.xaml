<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="BabyMobile.Views.EditFeedingPage">
    <StackLayout>
        <Entry Text="{Binding Feeding.Name}"></Entry>
        <Entry Text="{Binding Feeding.CountMilk}"></Entry>
        <Entry Text="{Binding Feeding.TimeMilk}"></Entry>
        <Entry Text="{Binding Feeding.DeviceId}"></Entry>

        <Button Command="{Binding EditCommand}"
                Text="Edit feeding"></Button>

        <Label Text="{Binding Message}"/>

        <Button Command="{Binding DeleteCommand}"
                Text="Delete feeding"/>

        <Label Text="{Binding DeleteMessage}"/>

        <Button Text="Go to Babies Page" Clicked="Button_Clicked"></Button>
    </StackLayout>
</ContentPage>